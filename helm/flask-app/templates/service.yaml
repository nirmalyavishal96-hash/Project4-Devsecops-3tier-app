apiVersion: v1
kind: Service
metadata:
  name: flask-service
  labels:
    app.kubernetes.io/name: flask-app
spec:
  type: {{ .Values.service.type }}
  selector:
    app: flask-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
      name: http